import Creative from "lib/Creative"; 
import "./less/main.less";

new Creative({ 
    brand: "{{brand.name}}", 
    campaign: "{{campaign.name}}", 
    format: "{{slug}}",
    slug : "{{path}}", 
    publisher: "{{publisher}}", 
    version: 1 
});