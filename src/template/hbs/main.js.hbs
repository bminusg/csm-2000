import Creative from "lib/creative"; 
import "lib/less/creatives/{{slug}}.less";

new Creative({ 
    brand: "{{brand.name}}", 
    campaign: "{{campaign.name}}", 
    format: "{{slug}}",
    slug : "{{path}}",
    publisher: "{{publisher}}",
    version: 1
});