{{#equal format.slug "ra"}}
// IMPORTS 
@use "lib/sass/abstracts/easings" as ease 
@use "node_modules/tween-sass" as * with ($parent: "#flyTarget", $activeClass: ".flyContentActive")

// CUSTOM CSS
#pFA


{{else}}
// IMPORTS 
@use "lib/sass/components/creative" with ($width:{{#if format.width}}{{format.width}}px{{else}}100%{{/if}}, $height:{{#if format.height}}{{format.height}}px{{else}}100%{{/if}}, $format:"{{format.slug}}")
@use "lib/sass/abstracts/easings" as ease 
@use "node_modules/tween-sass" as * 

// CUSTOM CSS
.creative

{{/equal}}